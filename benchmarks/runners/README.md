# Benchmark Runners

Executable benchmark entry points live here. During migration,
`scripts/bench.py`, `scripts/plot_bench.py`, and `scripts/plot_sweep.py` remain
the active runners; this directory is the target home for their replacement or
thin wrappers.
