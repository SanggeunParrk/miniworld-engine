# Retired runtime caches for 2.0.0

These measured entries no longer match the implementation/source keys. They are archived unchanged, not represented as rebuilt. Runtime uses the normal cache-miss policy until a new architecture-specific build is merged. A100 stale records remain under the existing unavailable-hardware exception.
