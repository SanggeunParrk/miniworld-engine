Experimental B7 source snapshot, not selected. Paired 800 samples: cached stats gains below 1%; stats/early-weight/mask regresses L768. Three consumers spill registers and were rejected before launch. SS recompute with two consumers is slower. Active B7 files and plans.py restored byte-for-byte from ../trimul_recompute_optimized_20260920 (relative to runs). Logs, config and ptxas artifacts retained in parent directory. To reproduce experimental variants use these files in a sibling runs directory with the remaining parent harness dependencies.
