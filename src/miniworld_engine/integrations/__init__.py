"""Adapters that run someone else's kernels behind this engine's module API.

Each adapter states what it loads, where it came from and what it refuses; none of them reimplement the upstream algorithm.
"""
