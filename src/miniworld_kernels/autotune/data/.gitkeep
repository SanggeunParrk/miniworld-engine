# Shipped per-GPU autotune config caches live here as <op>/<gpu_key>.json.
# Populate on-target with `python -m miniworld_kernels.autotune.build --op <op>` then
# copy the generated runtime cache here and commit.
